# 第一阶段：上游请求写入优化任务

## 目标

提升 scp0005 的**请求写入 TPS**（写入 MySQL(B) 请求表的速度）至 **10000+ TPS**。

---

## 达标标准

| 指标 | 目标值 |
|------|--------|
| 请求写入 TPS | ≥ 10000 |
| 错误率 | < 1% |

---

## 背景

### 系统架构

```
客户端 → scp0005 (外网网关) → MySQL(B) 请求表
              ↑
         ① 接收请求
         ② 写入数据库
```

scp0005 是整个穿透网关系统的入口，负责：
1. 接收客户端 HTTP 请求
2. 将请求写入内网 MySQL(B) 的 `inner_request` 表
3. 轮询 Redis 等待响应结果

### 网络配置

| 机器 | 角色 | IP 地址 | 说明 |
|------|------|---------|------|
| 机器 A | 外网 | 192.168.123.66 | 部署 scp0005, outer-consumer, Registry |
| 机器 B | 内网 | 192.168.123.81 | 部署 scp0006, target-service |

出现部署问题，用 `docker -H tcp://192.168.123.66:2375` 或 `docker -H tcp://192.168.123.81:2375` 去远程机器排查问题

### 当前性能基线

| 环节 | 操作 | 数量 | QPS/TPS |
|------|------|------|---------|
| ① | scp0005 接收请求 | 20,436 | 340.6 req/s |
| ② | 写入 MySQL(B) 请求表 | 20,436 | 340.6 TPS |

**问题**：当前上游写入速度仅 ~340 TPS，即使下游优化到 10000+ RPS，整体系统吞吐量也会被上游限制。

---

## 构建与测试

```bash
# 1. 部署
./deploy-registry.sh all

# 2. 重置统计
curl -X POST http://192.168.123.66:8080/api/stats/reset

# 3. 运行压测（并发1000，持续60秒，写入模式）
cd perf-test && python3 load_test.py 1000 60 http://192.168.123.66:8080 --write-only

# 4. 获取上游统计（关注 mysqlWriteSuccess 指标）
curl -s http://192.168.123.66:8080/api/stats | jq .
```

---

## 重要约束

1. 每轮优化后，必须将优化内容和本轮优化效果追加到 `perf-test/optimization-iterations.md`
2. **禁止运行 mvn 命令**，本地无 Maven，所有构建通过 `./deploy-registry.sh all` 完成
3. 本阶段仅优化上游（scp0005），不涉及下游服务

---
