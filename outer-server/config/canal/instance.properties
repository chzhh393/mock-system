# Canal 实例配置 - 外网（机器A）
# 监听外网 MySQL 的 outer_response 表

#################################################
## MySQL 数据源配置
#################################################
canal.instance.master.address=mysql-outer:3306
canal.instance.dbUsername=canal
canal.instance.dbPassword=canal123
canal.instance.connectionCharset=UTF-8

# 监听的数据库和表（正则表达式）
# 只监听 outer_gateway.outer_response 表
canal.instance.filter.regex=outer_gateway\\.outer_response

# 黑名单（不监听的表）
canal.instance.filter.black.regex=

#################################################
## Binlog 位置配置
#################################################
# 从最新位置开始
canal.instance.gtidon=false

#################################################
## MQ 配置（Kafka）
#################################################
canal.mq.topic=outer_response_binlog
canal.mq.partition=0

# 消息格式
canal.mq.flatMessage=true
