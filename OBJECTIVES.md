# 内外网穿透网关性能目标

> **基于分支**：`main`（同步轮询模式）
>
> 异步优化版本见 `feature/async-sinks` 分支

## 一、业务背景

- **正常 SLA**：端到端响应 p99 < 5s
- **异常场景**：部分 code 的下游接口变慢（10s~100s）
- **期望行为**：慢接口快速失败，不影响正常接口

---

## 二、整体目标

> 穿透网关在下游部分接口极慢（10s~100s）时，仍能持续接入请求，保证系统稳定不雪崩。

| 目标 | 量化指标 | 验证方法 |
|------|----------|----------|
| **不雪崩** | 慢接口 100 并发时，正常接口 p99 < 基线 × 1.5 | 混合压测 |
| **持续接入** | 新请求接入延迟 p99 < 200ms | 监控 T1 时间点 |
| **快速失败** | 超时后 < 100ms 返回错误响应 | 压测验证 |
| **不丢请求** | 100% 请求有明确响应（成功或失败） | 请求-响应配对统计 |

---

## 三、分段目标

### 3.1 scp0005 验收重点：非阻塞接入

**当前状态（main 分支）**：同步等待模式
- 使用 WebFlux + R2DBC（技术上非阻塞 I/O）
- 但业务逻辑是：请求 → 写 DB → **轮询 Redis 等待结果** → 返回
- 客户端需等待整个流程完成，下游慢时请求会堆积

| 验收项 | 指标 | 当前状态 |
|--------|------|----------|
| 请求接入与等待分离 | 写入 DB 后即返回受理 | ❌ 当前同步等待 |
| 轮询不占线程 | 1000 并发轮询时线程数不增长 | ✅ Reactor 模型 |
| 超时快速返回 | 超时后 < 50ms 返回错误 | ✅ Mono.timeout() |
| 资源可控 | 内存增长 < 基线 × 2 | ⚠️ 待验证 |
| 连接池不耗尽 | 下游慢时连接池不被占满 | ⚠️ 待验证 |

**目标**：
- 短期：验证当前同步模式下的资源瓶颈（连接数、内存、线程）
- 长期：改造为异步模式（写 DB 即返回，结果异步回调）

**测试场景**：
```bash
# 场景1：验证当前同步模式的极限
MOCK_DELAY_MS=30000  # 下游 30s 延迟
wrk -t4 -c100 -d60s http://192.168.123.66:8080/inner/c1/yhzx
# 观察：100 并发 × 30s 等待 = 持续 3000 个请求挂起
# 关注：内存、连接池、线程数是否爆掉

# 场景2：验证超时返回
MOCK_DELAY_MS=60000  # 下游 60s（超过 30s 超时）
# 观察：30s 后应返回超时错误
```

### 3.2 scp0006 验收重点：隔离下游慢、吞吐可控

| 验收项 | 指标 | 实现状态 |
|--------|------|----------|
| 慢接口隔离 | 慢 code 不影响快 code 的 p99 | ❌ 待实现 |
| 消费线程不被全占 | 至少保留 1 个可用消费者 | ❌ 待实现 |
| 熔断保护 | 慢调用率 >80% 时熔断 | ❌ 待实现 |
| 背压控制 | Kafka 消费可暂停 | ❌ 待实现 |

**测试场景**：
```bash
# 场景1：部分 code 慢（隔离验证）
yhzx: MOCK_DELAY_MS=30000  # 慢接口 30s
zdzx: MOCK_DELAY_MS=50     # 正常接口 50ms
# 验证：zdzx 的 p99 不受 yhzx 影响

# 场景2：持续慢触发熔断
yhzx 持续响应 >10s → 熔断触发 → 后续 yhzx 请求快速失败
# 验证：熔断后 yhzx 请求 <100ms 返回错误
```

---

## 四、验收测试矩阵

| 测试场景 | scp0005 验收点 | scp0006 验收点 |
|----------|----------------|----------------|
| 全部正常 | 基线性能 | 基线性能 |
| 单 code 慢 | 接入不阻塞 | 隔离生效 |
| 多 code 慢 | 资源不泄漏 | 熔断生效 |
| 全部慢 | 持续接入 | 快速失败 |
| 慢后恢复 | 无影响 | 半开恢复 |

---

## 五、关键指标监控

```
T1: scp0005 接收请求时间戳
T3: scp0006 消费 Kafka 时间戳
T4: target-service 响应时间戳
T6: outer-consumer 消费 Kafka 时间戳

CDC 延迟(内网) = T3 - T1
目标服务耗时 = T4 - T3
CDC 延迟(外网) = T6 - T4
端到端延迟 = Redis 获取时间 - T1
```
